{
  "name": "axion-contract-intelligence",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web"
  },
  "devDependencies": {
    "supabase": "2.114.0"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "franc-min": "^6.2.0",
    "mammoth": "^1.12.1",
    "pdfjs-dist": "^6.2.108"
  }
}
